此处存储的是我生产环境的Nginx配置文件的复制文件。

原文件位于：/etc/nginx



更新nginx步骤：

1. 检查

   ```bash
   nginx -t
   ```

2. 重启

   ```bash
   systemctl reload nginx
   ```

3. 重启（备选）

   ```bash
   systemctl restart nginx
   ```





