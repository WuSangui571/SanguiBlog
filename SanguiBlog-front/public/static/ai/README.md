# AI 助手 Logo 资源说明

- 放置目录：`SanguiBlog-front/public/static/ai/`
- 推荐文件名：`assistant-logo.png`
- 当前默认引用路径：`/static/ai/assistant-logo.png`

说明：

- 只要把你的自定义图片放到这个目录，并命名为 `assistant-logo.png`，前端 AI 助手会自动使用它。
- 建议使用正方形图片，推荐尺寸至少 `256x256`。
- 支持透明背景 PNG。
- 如果该文件不存在，前端会自动回退到默认的机器人图标，不会报错。
